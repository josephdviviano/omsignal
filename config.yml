data: '/rap/jvb-000-aa/COURS2019/etudiants/data/omsignal/myHeartProject/'
preprocessing:
  noise_gain: 1
dataloader:
  batch_size: 64
  shuffle: True
  num_workers: 6
models:
  lstm:
    sequence_len: 7499
    embedding_dim: 30
    hidden_dim: 1000
    num_layer: 3
    out_dims:
      - 1
      - 1
      - 1
      - 32
    out_types:
      - 'regression'
      - 'regression'
      - 'regression'
      - 'classification'
training:
  epochs: 100
  learning_rate: 0.01
  momentum: 0.9
  l2: 0
